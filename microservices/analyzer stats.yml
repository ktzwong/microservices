info:
  name: analyzer stats
  type: http
  seq: 5

http:
  method: GET
  url: http://localhost:8025/stats
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
